<section
  id="home"
  class="relative min-h-screen flex items-center justify-center overflow-hidden pt-20"
>
  <!-- Dynamic Background -->
  <div class="absolute inset-0 z-0">
    <div class="absolute inset-0 bg-navy opacity-90"></div>
    <div
      class="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] bg-gold/10 rounded-full blur-[120px]"
    >
    </div>
    <div
      class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-gold/5 rounded-full blur-[120px]"
    >
    </div>
  </div>

  <div class="container relative z-10 px-6 mx-auto">
    <div class="max-w-4xl mx-auto text-center">
      <div
        class="inline-block px-4 py-1.5 mb-6 text-sm font-medium tracking-wider text-gold uppercase bg-gold/10 rounded-full border border-gold/20 animate-fade-in"
      >
        Excelencia Legal en La Fortuna
      </div>

      <h1
        class="text-5xl md:text-7xl lg:text-8xl font-bold text-white leading-tight mb-8"
      >
        <span class="block">Asesoría Legal</span>
        <span class="text-gold italic font-serif">de Confianza</span>
      </h1>

      <p
        class="text-xl md:text-2xl text-gray-300 mb-12 max-w-2xl mx-auto leading-relaxed text-pretty"
      >
        Brindando servicios legales y notariales con <span
          class="text-white font-semibold">integridad</span
        >,
        <span class="text-white font-semibold">eficiencia</span> y un compromiso inquebrantable
        con su éxito.
      </p>

      <div class="flex flex-col sm:flex-row gap-5 justify-center items-center">
        <a
          href="#contact"
          class="group relative inline-flex items-center justify-center px-8 py-4 font-semibold text-navy transition-all duration-300 bg-gold rounded-full hover:bg-gold-light hover:scale-105 active:scale-95 shadow-lg shadow-gold/20"
        >
          <span>Agendar Consulta</span>
          <svg
            class="w-5 h-5 ml-2 transition-transform duration-300 group-hover:translate-x-1"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>

        <a
          href="#about"
          class="inline-flex items-center justify-center px-8 py-4 font-semibold text-white transition-all duration-300 border border-white/20 rounded-full hover:bg-white/10 hover:border-white/40"
        >
          Ver Servicios
        </a>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-10 left-1/2 -translate-x-1/2 animate-bounce">
    <div
      class="w-6 h-10 border-2 border-white/20 rounded-full flex justify-center pt-2"
    >
      <div class="w-1 h-2 bg-gold rounded-full"></div>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-in {
    animation: fade-in 0.8s ease-out forwards;
  }
</style>
